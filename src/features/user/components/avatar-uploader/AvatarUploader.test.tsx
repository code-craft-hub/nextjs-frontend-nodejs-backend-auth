import { describe, it } from "vitest";

describe("AvatarUploader", () => {
  it.todo("renders upload button");
  it.todo("shows preview after selecting an image");
  it.todo("calls onUpload with the selected file");
  it.todo("displays error when file size exceeds limit");
});
