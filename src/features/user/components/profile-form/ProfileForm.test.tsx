import { describe, it } from "vitest";

describe("ProfileForm", () => {
  it.todo("renders all profile fields");
  it.todo("validates required fields before submit");
  it.todo("calls onSubmit with form data on valid input");
  it.todo("shows loading state while submitting");
});
