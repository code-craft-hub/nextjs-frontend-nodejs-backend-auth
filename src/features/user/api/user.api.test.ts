import { describe, it } from "vitest";

describe("user.api", () => {
  it.todo("getProfile — returns user profile on 200");
  it.todo("updateProfile — sends PUT and returns updated user");
  it.todo("uploadAvatar — uploads FormData and returns photoURL");
  it.todo("deleteAccount — sends DELETE and clears session");
});
